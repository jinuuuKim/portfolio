# 로컬 미리보기용 스테이징 폴더 (pages.yml이 만드는 것과 동일한 구조)
_site/

# 내부 작업 문서 — 절대 깃허브에 올리지 않는다 (로컬 전용)
SITE-SPEC.md

# 이미지 배치 안내 — website/ 안에 있으면 사이트로 같이 배포돼 공개된다.
# 내부 메모(원본 페이지 번호·수치 충돌 경고 등)라 배포 대상에서 제외한다.
website/assets/img/README.md

# 미사용 이미지 임시 보관함.
# 작업 중 안 쓰게 된 파일을 여기로 옮겨두면 김진우가 확인 후 삭제한다.
# website/ 하위는 Pages로 그대로 배포되므로 반드시 커밋에서 제외한다.
website/assets/img/_unused/
